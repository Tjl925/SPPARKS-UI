# 首版验证记录

日期：2026-09-09。

## 自动检查

- Python unittest：4 项通过。覆盖历史 dump 与能量对齐、无序 ID 对齐、固定格点约束、非法参数和脚本模板。
- Node test：2 项通过。覆盖有效结果导入、缺失能量和异常数据拒绝。
- JavaScript 语法检查：app.js、viewer.js、result-schema.js 通过。
- HTTP 检查：越界参数 400、外部 Origin 403、不存在任务 404、静态文件路径越界 404。

## 实际引擎

- 使用本机 Ubuntu-24.04 WSL 和已有 spk_serial。
- API 和页面表单各运行一次 8×8×8、Q=20、T=0、seed=56789、终止时间 10 的 Potts 算例。
- 均成功生成 512 格点、11 帧；初始能量 12662，最终能量 4544。
- 页面提交任务后显示计算中状态，完成后自动载入结果，并切换“本次计算”来源标注。

## 浏览器交互

- 无浏览器控制台 error / warn。
- 历史结果显示 8000 格点、11 帧；初始能量 205912，最终能量 25382。
- X 剖切至第 0 层，显示 400 / 8000 格点；方向与位置控件正确启用。
- 双视图展示真实随机初态和末态，拖拽旋转时两侧视角同步。
- 点击格点显示 ID、状态、坐标和对应模型时间。
- 时间轴与逐帧按钮联动能量；自动播放推进到 t=20，能量 56802，暂停后停止回放。
- 实際 result.json 通过文件选择器导入，显示 512 格点、11 帧并标注“用户提供”。
- PNG 导出文件实际生成，已打开检查三维图和时间/来源标注。
- 390×844 窄屏检查：无横向溢出，主视图、时间轴及下方控制区可使用。测试后恢复原视口。
- 展示模式、案例入口、恢复原始示例已检查。

## 明确边界

这是本地首版，先覆盖 Potts 固定三维晶格；不包含铁电求解器、矢量场渲染、暂停求解、多任务结果管理或实时逐帧推送。
当前执行程序来自用户已有目录，没有重新编译源码。模型描述与结果契约已预留后续扩展位置。
