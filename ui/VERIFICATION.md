# 验证记录

## 首次使用引导与恢复体验

- 自动检查：8 项 Python、6 项 Node 通过；新增小数步长、沉积量组合、提交状态不确定、错误恢复说明测试。
- 浏览器：首次引导、下一步、收起后刷新保持隐藏、从“使用引导”重开验证通过。
- 输入尺寸 4 被阻止提交，提示正确范围 8–32；定位按钮聚焦出错输入并标记无效。
- 修正为 8³、终止时间 10 后真实 Potts 运行完成（5 秒、11 帧），成功提示可跳到结果。
- 在任务完成后停止测试服务，切换 Ising 触发载入失败；原计算结果保留。恢复服务并点“重新载入案例”成功加载 Ising。
- 390px 窄屏引导完整可读，无横向页面溢出。任务运行失败/提交不确定的解释分支由自动测试覆盖，未人为中断真实求解进程。
- 新版工作台与新手引导均已由用户明确验收通过；多案例按用户要求继续保持未验收。

## 2026-09-17 专业工作台重构

- Python unittest 8 项、Node 结果校验 3 项、app.js 语法检查通过。新增日志限长/部分输出和硬件探测失败降级测试。
- `tests/smoke_api.py`：Potts / Ising / 薄膜各真实执行成功（512 / 512 / 768 格点，均 11 帧）；任务接口包含真实 SPPARKS 日志；平台接口明确串行资源。
- 浏览器页面真实提交 Potts（8³，终止模型时间 10），5 秒完成、11 帧。计算时参数和模型选择锁定，原结果保留且有提示；完成后载入 512 格点并保留日志。
- 核对硬件检测：宿主机 6 物理核心、12 逻辑处理器，内存以 GiB 表达并标注采样时刻；任务资源单独标注 1 进程/1 线程。
- 三案例参数分组和说明正常切换；薄膜小数/极小数范围显示正确，完整晶格显示 1,536 格点，初始/当前对比正常。
- 修改输入后显示旧结果提示；控制台启动与可视化回放分离；观察工具可展开。
- 390px 窄屏与 1440px 桌面检查，无页面横向溢出。后续用户已明确确认新版工作台验收通过；首次使用引导与多案例验收另行记录。

内存为服务启动时的宿主机快照，不是实时任务监控；当前不包含 MPI 资源配置或运行中帧推送。

## 2026-09-14 多案例扩充验证

- 自动测试：6 项 Python + 3 项 Node 通过，包含新案例与原始 dump/日志的一致性、模型参数隔离、模型状态值和二维几何校验。
- `tests/smoke_api.py`：Potts、Ising、薄膜各提交一个真实任务并成功取得11帧；未知模型与混用参数被拒绝。
- 新随包 Ising：8,000 格点，11 帧；SPPARKS 能量从24,104变为9,044，末态平均自旋约−0.734。
- 新随包薄膜：1,536 格点，11 帧，占据数由96增至487。原非线性模型默认能量表为零，主曲线使用实际占据数。
- 浏览器：三卡片入口、Ising两色视图及独立参数、薄膜二维视图、显隐空位（96/487/1536）、时间轴和初始/当前对比已检查。
- 页面提交薄膜小算例成功，完成后加载11帧；由薄膜导入Ising结果时，维度、参数、图例及来源正确切换。
- 模型说明和导出入口已检查，未观察到浏览器控制台 error/warn。
- 新案例使用本地 SPPARKS (08 Oct 2025) 生成；源输入、日志、dump和JSON保留在demo目录。

以下为历史首版验证记录，保留其当时的范围与结论。

日期：2026-09-09。

## 自动检查

- Python unittest：4 项通过。覆盖历史 dump 与能量对齐、无序 ID 对齐、固定格点约束、非法参数和脚本模板。
- Node test：2 项通过。覆盖有效结果导入、缺失能量和异常数据拒绝。
- JavaScript 语法检查：app.js、viewer.js、result-schema.js 通过。
- HTTP 检查：越界参数 400、外部 Origin 403、不存在任务 404、静态文件路径越界 404。

## 实际引擎

- 使用本机 Ubuntu-24.04 WSL 和已有 spk_serial。
- API 和页面表单各运行一次 8×8×8、Q=20、T=0、seed=56789、终止时间 10 的 Potts 算例。
- 均成功生成 512 格点、11 帧；初始能量 12662，最终能量 4544。
- 页面提交任务后显示计算中状态，完成后自动载入结果，并切换“本次计算”来源标注。

## 浏览器交互

- 无浏览器控制台 error / warn。
- 历史结果显示 8000 格点、11 帧；初始能量 205912，最终能量 25382。
- X 剖切至第 0 层，显示 400 / 8000 格点；方向与位置控件正确启用。
- 双视图展示真实随机初态和末态，拖拽旋转时两侧视角同步。
- 点击格点显示 ID、状态、坐标和对应模型时间。
- 时间轴与逐帧按钮联动能量；自动播放推进到 t=20，能量 56802，暂停后停止回放。
- 实際 result.json 通过文件选择器导入，显示 512 格点、11 帧并标注“用户提供”。
- PNG 导出文件实际生成，已打开检查三维图和时间/来源标注。
- 390×844 窄屏检查：无横向溢出，主视图、时间轴及下方控制区可使用。测试后恢复原视口。
- 展示模式、案例入口、恢复原始示例已检查。

## 明确边界

这是本地首版，先覆盖 Potts 固定三维晶格；不包含铁电求解器、矢量场渲染、暂停求解、多任务结果管理或实时逐帧推送。
当前执行程序来自用户已有目录，没有重新编译源码。模型描述与结果契约已预留后续扩展位置。
