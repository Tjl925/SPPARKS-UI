feat: add accepted 3D Potts material simulation workbench

Build a case-driven SPPARKS UI for presenter-led material simulation demos.
Display real 3D Potts output with orbit, zoom, pan, clipping, site picking,
timeline playback, linked energy charts, and synchronized initial/current views.

Connect the local WSL SPPARKS executable through a bounded Python job runner.
Generate parameterized inputs in isolated run folders and load real results
after completion. Add JSON result import/export, annotated PNG export,
model metadata, validation, and extension contracts for future material models.

Document setup, scientific interpretation, verification, and the full project
roadmap with completed items checked for 2026-09-09.

Validation:
- 4 Python tests and 2 Node tests passed.
- 4 HTTP validation checks passed.
- 2 real 512-site, 11-frame Potts runs completed successfully.
- Browser checks covered 3D interaction, clipping, picking, comparison,
  playback, import, PNG output, and a narrow-screen layout.
- User accepted the first version on 2026-09-09.

Current scope: Potts fixed-lattice categorical states; results load after
computation. Ferroelectric solvers, vector fields, live frame streaming,
and persistent experiment history remain planned work.
